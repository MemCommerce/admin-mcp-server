[project]
name = "admin-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>1.9.3",
    "modular-mcp>=0.3.0",
    "pillow>=11.2.1",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]

[project.scripts]
admin-mcp-server = "admin_mcp_server:main"
